# opencode-ntfy-approve
OpenCode plugin that sends tool-permission requests to your phone via ntfy, with Allow/Reject buttons in the notification. Tap to respond — no desktop needed. The only OpenCode+ntfy plugin with interactive approval.
